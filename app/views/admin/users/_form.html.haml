.col-sm-5.col-sm-offset-3
	= simple_form_for([:admin, @user], html:{class:'form-horizontal'}, wrapper: :horizontal_form) do |f|
		= f.input :student_id, label: "学号"
		= f.input :username, label: "用户昵称"
		= f.input :classgrade, label: "班级"
		= f.input :dormitory, label: "宿舍"
		= f.input :phone, label: "电话"
		= f.input :signature, label: "个性签名"
		= f.button :submit, value: "提交", class: "col-sm-4 col-sm-offset-4 btn-primary"
