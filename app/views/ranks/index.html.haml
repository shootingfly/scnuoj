%table.table.table-hover.datatable{"data-source" => "#{ranks_path(format: :json)}"}
	%thead
		%tr
			%th= t('Rank')
			%th= t('User')
			%th= t('Class')
			%th= t('Dormitory')
			%th= t('Score')
	%tbody
