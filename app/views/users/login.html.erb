<!-- <h4>登录</h4>
<%= simple_form_for(create_login_session_path, html:{class:'form-horizontal'}, wrapper: :horizontal_form) do |f|  %>
	<%= f.input :student_num, label: "学号" %>
	<%= f.input :password, label: "密码" %>
	<%= f.button :submit, value: "登录", class: "col-sm-4 col-sm-offset-4 btn-primary" %>
<% end %> -->

<div class="login-form-container clearfix">
  <div class="login-form">
    <%= form_tag "/create_login_session" do %>
      <dl class="form">
        <dt>
          <%= label_tag "用户名" %>
        </dt>
        <dd>
          <%= text_field_tag :student_id, params[:student_id] %>
        </dd>
      </dl>
      <dl class="form">
        <dt>
          <%= label_tag "密码" %>
        </dt>
        <dd>
          <%= password_field_tag :password, params[:password] %>
        </dd>
      </dl>
      <dl class="form remember-me">
  <%= check_box_tag :remember_me, 1, params[:remember_me] %>
  <%= label_tag :remember_me %>
</dl>
      <p> <%= submit_tag "登录", :class => "login-button btn btn-primary" %> </p>
      </div>
    <% end %>
  </div>
</div>
